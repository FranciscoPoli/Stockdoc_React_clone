{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ],
  "buildCommand": "cd client && npm install && npm run build && cd .. && npm install && tsc server/index.ts --outDir dist && cp -r client/dist/* dist/",
  "outputDirectory": "dist",
  "framework": "vite"
}
