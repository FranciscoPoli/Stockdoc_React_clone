{
  "buildCommand": "cd client && npm install && npm install wouter@3.0.0 use-sync-external-store@1.2.0 --save && NODE_ENV=production vite build --config vite.config.ts && cd .. && mkdir -p dist && cp -r client/dist/* dist/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
