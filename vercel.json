{
  "buildCommand": "cd client && npm install && npm install use-sync-external-store@1.2.0 --save && VITE_SKIP_WARNINGS=true NODE_ENV=production vite build --mode production && cd .. && mkdir -p dist && cp -r client/dist/* dist/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
